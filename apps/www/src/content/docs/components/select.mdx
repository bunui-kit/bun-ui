---
title: Select
description: A dropdown component for selecting a single option from a list.
links:
  source: components/select
  docs: /docs/components/select
---

<ComponentPreview name="select-basic" />

## Usage

Import the `Select` component and its related subcomponents from `@bun-ui/react`
and use them in your project:

```tsx
import { Select, SelectItem } from "@bun-ui/react"
```

```tsx
<Select placeholder="Select an option">
  <SelectItem value="option1">Option 1</SelectItem>
  <SelectItem value="option2">Option 2</SelectItem>
  <SelectItem value="option3">Option 3</SelectItem>
</Select>
```

## Examples

### Groups and Labels

The `Select` component supports grouping options with labels for better
organization.

<ComponentPreview name="select-grouped" />

### Controlled

<ComponentPreview name="select-controlled" />

### Disabled

The `Select` comonent can be disable as a whole or individual items can be
disabled.

<ComponentPreview name="select-disabled" />

### Render Value

For more complex use cases, you can customize how the selected value is rendered on
the select trigger element by using `renderValue` prop.

<ComponentPreview name="select-render-value" />

### Customization

The `Select` component and its subcomponents can be customized using the `classes` prop.
This allows you to style each part of the select component independently.

<ComponentPreview name="select-customized" />

## Props

### Select

<APITable interfaceName="SelectProps" />

### SelectClasses

<APITable interfaceName="SelectClasses" />

### SelectItem

<APITable interfaceName="SelectItemProps" />

### SelectItemClasses

<APITable interfaceName="SelectItemClasses" />
