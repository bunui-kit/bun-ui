---
title: Typography
description: A component for displaying text with consistent styling and formatting.
links:
  source: components/typography
  docs: /docs/components/typography
---

<ComponentPreview name="typography-basic" />

## Usage

Import the `Typography` component from `@bun-ui/react` and use it in your project:

```jsx
import { Typography } from "@bun-ui/react"
```

```jsx
<Typography>Hello World</Typography>
```

## Examples

### Variants

The `Typography` component supports different variants for various text styles:

<ComponentPreview name="typography-variants" />

### Elements

You can render the Typography component as different HTML elements using the `as` prop:

<ComponentPreview name="typography-elements" />

### Font Sizes

The `Typography` component supports different font sizes:

<ComponentPreview name="typography-sizes" />

### Font Weights

You can adjust the font weight using the `fontWeight` prop:

<ComponentPreview name="typography-weights" />

### Truncate

Use the `truncate` prop to add text truncation with an ellipsis:

<ComponentPreview name="typography-truncate" />

## Props

### Typography

<APITable interfaceName="TypographyProps" />
