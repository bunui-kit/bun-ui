---
title: Icon Button
description: A square button for icon-only actions, supporting all color and size variants.
links:
  source: components/icon-button
  docs: /docs/components/icon-button
---

<ComponentPreview name="icon-button-basic" />

## Usage

Import the `IconButton` component from `@bun-ui/react` and use it in your project:

```jsx
import { IconButton } from "@bun-ui/react"
```

```jsx
<IconButton>
  <StarIcon />
</IconButton>
```

## Examples

### Color

The `IconButton` supports all the same color variants as `Button`:

<ComponentPreview name="icon-button-color" />

### Sizes

The `IconButton` supports three square sizes: `sm`, `md`, and `lg`:

<ComponentPreview name="icon-button-size" />

### As Child

You can use the `asChild` prop to render the IconButton as a different element:

<ComponentPreview name="icon-button-as-child" />

## Props

### IconButton

<APITable interfaceName="IconButtonProps" />
